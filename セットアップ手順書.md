# WidowX Sub Agent セットアップ手順書

## 前提条件

| 項目    | 要件                                                        |
| ------- | ----------------------------------------------------------- |
| OS      | Ubuntu 24.04 LTS（WidowX 制御 Linux PC）                    |
| Python  | 3.10 以上（`python3 --version` で確認）                     |
| Node.js | 18 以上（`node -v` で確認）                                 |
| GPU     | NVIDIA GPU（RTX 3000系以降推奨）                            |
| Ollama  | インストール済み（未インストールの場合はセクション7を参照） |

---

## 0. Linux PC のユーザー構成について

このシステムは **各自のホームディレクトリに git clone して使う** 方式を採用しています。  
`sudo userB` のような特定ユーザー依存のコマンドは不要です。

```
/home/userA/widowx_system/   ← Aさん用
/home/userB/widowx_system/   ← Bさん用
```

画像認識ツール（`widowx_reaction_tool`）はリポジトリに含まれているため、  
`git clone` するだけですぐに動作します。

---

## 1. git clone

```bash
git clone https://github.com/BeBeBecon/WidowX_Demo_System.git ~/widowx_system
cd ~/widowx_system
```

> **既存環境の更新（2回目以降）:**  
> すでに clone 済みの場合は `git pull` するだけで OK です。  
> `.env` は git 管理外のため個人設定は保持されます。

---

## 2. LeRobot（lerobot_trossen）セットアップ

### 2-1. uv のインストール

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
source ~/.bashrc   # または ~/.zshrc
which uv           # パスを確認しておく（後で .env に記載）
```

> `setup.sh`（セクション3）も uv を自動インストールしますが、LeRobot の `uv sync`（次のセクション2-2）が先に必要なため、ここで手動インストールが必要です。

### 2-2. lerobot_trossen の clone とインストール

```bash
git clone https://github.com/TrossenRobotics/lerobot_trossen.git ~/lerobot_trossen
cd ~/lerobot_trossen
uv sync
```

### 2-3. インストール確認

```bash
cd ~/lerobot_trossen
uv pip list | grep lerobot
```

"LeRobot"と表示されればOK。

> **⚠️ uv sync について（重要）**  
> `uv sync` は初回インストールのみ実行してください。  
> **2回目以降は実行しないこと。** 手動インストール済みの torch（特に Blackwell 対応版）が古いバージョンに戻ります。  
> パッケージを追加する場合は `uv pip install` を使ってください。

### 2-4. RTX 5000系（Blackwell）GPU の場合のみ

RTX 5080 / 5090 / 5070 など Blackwell アーキテクチャの GPU では、標準 PyTorch が CUDA カーネルエラーを起こします。  
上記 `uv sync` 完了後、続けて以下を実行してください。

```bash
cd ~/lerobot_trossen

# CUDA 12.8 対応 Nightly 版 PyTorch を強制インストール
VIRTUAL_ENV=.venv uv pip install --force-reinstall --pre \
  torch torchvision torchaudio \
  --index-url https://download.pytorch.org/whl/nightly/cu128

# インストール確認（sm_100 が含まれていればOK）
uv run --no-sync python -c "import torch; print(torch.__version__); print(torch.cuda.get_arch_list())"
```

> **この手順の後は `uv sync` を絶対に実行しないこと**（torch が古い版に戻るため）。

---

## 3. セットアップスクリプト実行

```bash
cd ~/widowx_system
bash setup.sh
```

setup.sh が自動で行うこと：

- Python バージョン確認
- GPU / CUDA バージョン確認（RTX 5000系の場合は追加手順を案内）
- `uv` 自動インストール（未インストールの場合）
- Ollama + `qwen2.5:3b` 確認・自動取得
- `.env` ファイルの生成（`.env.example` からコピー）
- バックエンド Python 仮想環境の構築（`huggingface-hub` 等の依存パッケージを含む）
- フロントエンド npm パッケージのインストール
- `tools/*.sh` への実行権限付与

---

## 4. 環境変数の設定（`.env`）

`setup.sh` が自動で `.env` を生成しますが、各自の環境に合わせて値を書き換えてください。

```bash
# .env のパスを確認
ls ~/widowx_system/.env
```

ファイルマネージャーまたはお好みのテキストエディタで `~/widowx_system/.env` を開き、以下の値を変更してください：

```bash
# 自分のユーザー名・ホームディレクトリの確認
whoami      # ユーザー名
echo $HOME  # ホームディレクトリのフルパス
```

| 変数名         | 説明                                    | 変更例                        |
| -------------- | --------------------------------------- | ----------------------------- |
| `LEROBOT_PATH` | lerobot_trossen のパス                  | `/home/userA/lerobot_trossen` |
| `ROBOT_IP`     | ロボットアームの IP（変更不要な場合も） | `192.168.1.4`                 |
| `HF_USER`      | HF ユーザー名（eval 保存先）            | `your_hf_username`            |
| `UV_PATH`      | uv のフルパス                           | `/home/userA/.local/bin/uv`   |
| `DRY_RUN`      | 実機を動かす場合は `false` に変更       | `false`                       |

> HF アクセストークンは `.env` への記載不要です。次のセクション5で `login()` を実行するとキャッシュに保存され、以降の操作で自動的に参照されます。

```bash
# UV_PATH の確認方法
which uv
```

> `OLLAMA_HOST` は通常変更不要です（デフォルト: `http://localhost:11434`）。

---

## 5. Hugging Face ログイン

データセットのダウンロードには認証が必要なため、以下で**初回のみ**ログインしてください。

```bash
# 仮想環境が有効な場合は先に抜ける
deactivate 2>/dev/null || true

cd ~/lerobot_trossen
uv run huggingface-cli login
```

実行するとトークン入力を求められます。  
トークンは [https://huggingface.co/settings/tokens](https://huggingface.co/settings/tokens) で取得してください。

**トークン作成時に以下の権限を選択してください：**

| 権限                                                                         | 用途                               |
| ---------------------------------------------------------------------------- | ---------------------------------- |
| Read access to contents of all repos under your personal namespace           | データセットの読み取り             |
| View access requests for all gated repos under your personal namespace       | ゲート付きリポジトリのアクセス確認 |
| Write access to contents/settings of all repos under your personal namespace | データセットのアップロード         |
| Read access to all collections under your personal namespace                 | コレクションの参照                 |
| Write access to all collections under your personal namespace                | コレクションの更新                 |

※ 詳細に関してはテガラ社様のマニュアルを参照ください。

認証に関して質問がでることがございます。その場合は "yes" > Enter で進んでください。

```bash
# ログイン確認
uv run huggingface-cli whoami
```

> ログイン情報は `~/.cache/huggingface/token` に保存されます。  
> 2回目以降はこの手順は不要です。

---

## 6. データセットのダウンロード

```bash
bash ~/widowx_system/download_datasets.sh
```

ダウンロードされるデータセット（`docomoshiken1/` 配下）：

| データセット                          | 用途               |
| ------------------------------------- | ------------------ |
| `widowx_contactless_payment_20260602` | タッチ決済スキル   |
| `widowx_grab_bottle_20260528`         | ペットボトルを掴む |
| `widowx_pour_bottle_20260528`         | 水を注ぐ           |
| `grab_cube`                           | キューブを掴む     |
| `widowx_reaction_*_yes_20260528`      | うなずき×3方向     |
| `widowx_reaction_*_no_20260528`       | 首振り×3方向       |
| `widowx_wave_*_20260528`              | 手振り×3方向       |

保存先: `$HOME/.cache/huggingface/lerobot/docomoshiken1/<dataset名>/`

> **すでにダウンロード済みの場合:** ファイルのハッシュを比較して差分のみ更新されます。再実行しても全量ダウンロードにはなりません。

---

## 7. Ollama の管理　（LLMでの推論機能用）

Ollama は**システム全体で1つ**動作するサービスです。モデルは全ユーザー共通のため、誰か1人が `pull` すれば全員使えます。

> **本 Linux PC では導入済みのため、通常この手順は不要です。**  
> 新規環境にセットアップする場合のみ以下を実施してください。

```bash
# Ollama インストール（未インストールの場合のみ）
curl -fsSL https://ollama.com/install.sh | sh

# モデルの取得（setup.sh でも自動取得されます）
ollama pull qwen2.5:3b
```

---

```bash
# 状態確認
systemctl status ollama

# 一時停止（次回PC起動時は自動起動される）
sudo systemctl stop ollama

# 自動起動を無効化（共有PCでメモリを節約したい場合）
sudo systemctl disable ollama

# 手動で起動
sudo systemctl start ollama

# モデル一覧確認（qwen2.5:3b が表示されればOK）
ollama list
```

> 共有 PC の場合は `disable` しておき、デモ前に `start` するのを推奨。  
> `stop` / `start` は全ユーザーに影響するため、他の人が使用中でないか確認してください。

---

## 8. 起動方法

ターミナルを2つ開いて実行します。

**ターミナル1 - バックエンド:**

```bash
cd ~/widowx_system
bash start_backend.sh
```

**ターミナル2 - フロントエンド:**

```bash
cd ~/widowx_system
bash start_frontend.sh
```

ブラウザで `http://localhost:5278` を開く。

> **⚠️ 実機でロボットを動かす場合:**  
> `.env` の `DRY_RUN=false` になっていることを確認してください（`true` のままだとシミュレートモードで実際にアームは動きません）。

---

## 9. タッチ決済スキル用：別PCからのフロントエンド起動

Linux PC が有線接続のため、スマホ（無線LAN）と直接通信できない場合に使用します。  
MacBook など無線LAN に接続した別 PC で frontend を起動し、スマホの NFC タッチ時に表示するページを配信します。

> Linux バックエンドとの連携は不要です。MacBook 単独で frontend を起動します。

### ① MacBook に Node.js をインストール（未インストールの場合）

```bash
# Homebrew 経由
brew install node
```

### ② このリポジトリを MacBook に clone

```bash
git clone https://github.com/BeBeBecon/WidowX_Demo_System.git ~/widowx_system
cd ~/widowx_system/frontend
npm install
```

### ③ フロントエンド起動

```bash
npm run dev
```

### ④ NFC タグの URL を確認・更新

NFC タグには MacBook の IP アドレスを含む URL が書き込まれています。  
MacBook の IP を確認してください：

```bash
# Mac の場合
ipconfig getifaddr en0   # WiFi の IP
```

NFC タグの URL（例: `http://192.168.10.10:5278/payment-demo`）と一致しているか確認し、  
IP が変わった場合はスマホの「NFC Tools」等のアプリでタグ内の URL を書き換えてください。

---
